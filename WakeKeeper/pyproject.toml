[project]
name = "wake-keeper"
version = "0.1.0"
description = "A simple Amphetamine-like keep-awake menu bar app for macOS"
requires-python = ">=3.11"
dependencies = ["rumps>=0.4.0", "Pillow>=10.0"]

[tool.uv]
managed = true
package = false   # 不把当前目录当作可安装包
