[project]
name = "menu-bar-guard"
version = "1.0.0"
description = "A macOS menu bar spacer for keeping status items away from the notch"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0",
  "pyinstaller>=6.0",
  "pyobjc-framework-Cocoa>=10.0",
]

[tool.uv]
managed = true
package = false
